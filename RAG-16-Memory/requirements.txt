# ── Memory ────────────────────────────────────────────
mem0ai>=2.0.0          # v2.0 has breaking API changes (filters dict, top_k)
langmem>=0.0.25        # hot-path tools + background ReflectionExecutor

# ── LangGraph + LangChain ─────────────────────────────
langgraph>=1.0.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-pinecone>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# ── Vector DB ─────────────────────────────────────────
pinecone>=5.0.0

# ── PDF Loading ───────────────────────────────────────
pypdf>=5.0.0

# ── LLM Providers ─────────────────────────────────────
openai>=1.50.0

# ── UI ────────────────────────────────────────────────
streamlit>=1.40.0

# ── Utilities ─────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
